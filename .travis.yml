language: node_js
node_js:
  - "stable"
sudo: true
dist: trusty

branches:
  only:
  - master

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run lint
  - npm run test